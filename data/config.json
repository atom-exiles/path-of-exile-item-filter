{
  "generalSettings": {
    "title": "General",
    "type": "object",
    "order": 1,
    "properties": {
      "enableCompletion": {
        "title": "Enable Autocompletion",
        "description": "Enable autocompletion within loot filters.",
        "type": "boolean",
        "default": true,
        "order": 1
      },
      "enableLinter": {
        "title": "Enable Linter",
        "description": "Enable error checking on loot filters.",
        "type": "boolean",
        "default": true,
        "order": 2
      }
    }
  },
  "dataSettings": {
    "title": "Data",
    "type": "object",
    "order": 2,
    "properties": {
      "enableLeague": {
        "title": "League-specific Items",
        "description": "Allow the use of league-specific items within loot filters.",
        "type": "boolean",
        "default": true,
        "order": 1
      },
      "enableLegacy": {
        "title": "Legacy Items",
        "description": "Allow the use of items that are unobtainable in the current temporary leagues.",
        "type": "boolean",
        "default": false,
        "order": 2
      },
      "enableRecipe": {
        "title": "Vendor Recipe Items",
        "description": "Allow the use of items which are only obtainable through vendor recipes.",
        "type": "boolean",
        "default": false,
        "order": 3
      },
      "classWhitelist": {
        "title": "Item Class Whitelist",
        "description": "Allow the following additonal item classes to be used within filter files. Multiple entries should be separated by a comma.",
        "type": "array",
        "default": [],
        "order": 4,
        "items": { "type": "string" }
      },
      "baseWhitelist": {
        "title": "Item Base Whitelist",
        "description": "Allow the following additonal base types to be used within filter files. Multiple entries should be separated by a comma.",
        "type": "array",
        "default": [],
        "order": 5,
        "items": { "type": "string" }
      }
    }
  },
  "completionSettings": {
    "title": "Autocompletion",
    "type": "object",
    "order": 3,
    "properties": {
      "enableRightLabel": {
        "title": "Right Label",
        "description": "Enable the right label, which provides additional information.",
        "type": "boolean",
        "default": true,
        "order": 1
      },
      "enableIcon": {
        "title": "Icons",
        "description": "Enable the icon decoration.",
        "type": "boolean",
        "default": true,
        "order": 2
      }
    }
  },
  "linterSettings": {
    "title": "Linter",
    "type": "object",
    "order": 4,
    "properties": {
      "enableWarnings": {
        "title": "Warnings",
        "description": "Enable linter warnings within the editor.",
        "type": "boolean",
        "default": true,
        "order": 1
      },
      "performanceTips": {
        "title": "Performance Tips",
        "description": "Provide performance tips for loot filters as warnings.",
        "type": "boolean",
        "default": false,
        "order": 2
      },
      "method": {
        "title": "Method",
        "description": "Determines when filter files will be processed by the linter.",
        "type": "string",
        "default": "onEdit",
        "order": 3,
        "enum": [
          { "value": "onEdit", "description": "On Edit: process loot filters as they are modified." },
          { "value": "onSave", "description": "On Save: only process loot filters on save and load." }
        ]
      }
    }
  }
}
