{
  "general": {
    "title": "General",
    "type": "object",
    "order": 1,
    "properties": {
      "enableCompletion": {
        "title": "Enable Autocompletion",
        "description": "Enable autocompletion within item filters.",
        "type": "boolean",
        "default": true,
        "order": 1
      },
      "enableLinter": {
        "title": "Enable Linter",
        "description": "Enable error checking for item filters.",
        "type": "boolean",
        "default": true,
        "order": 2
      },
      "enableGutter": {
        "title": "Enable Editor Gutter",
        "description": "Enable the decoration gutter within the editor whenever an item filter is open.",
        "type": "boolean",
        "default": true,
        "order": 3
      },
      "chunkSize": {
        "title": "Validation Chunk Size",
        "description": "The number of item filter lines to process at once. If you encounter mousewheel lag when opening large files, then lower this number.",
        "type": "number",
        "default": 500,
        "order": 4
      }
    }
  },
  "data": {
    "title": "Data",
    "type": "object",
    "order": 2,
    "properties": {
      "dataset": {
        "title": "Dataset",
        "description": "Determines the dataset used by the package.",
        "type": "string",
        "default": "current",
        "order": 1,
        "enum": [
          { "value": "current", "description": "Current: load data for the current version of the game." }
        ]
      },
      "classWhitelist": {
        "title": "Item Class Whitelist",
        "description": "Allow the following additonal item classes to be used within filter files. Multiple entries should be separated by a comma.",
        "type": "array",
        "default": [],
        "order": 5,
        "items": { "type": "string" }
      },
      "baseWhitelist": {
        "title": "Item Base Whitelist",
        "description": "Allow the following additonal base types to be used within filter files. Multiple entries should be separated by a comma.",
        "type": "array",
        "default": [],
        "order": 6,
        "items": { "type": "string" }
      }
    }
  },
  "completion": {
    "title": "Autocomplete",
    "type": "object",
    "order": 3,
    "properties": {
      "enableExtraSuggestions": {
        "title": "Extra Suggestions",
        "description": "Enable extra suggestions, such as the ones for map tiers.",
        "type": "boolean",
        "default": true,
        "order": 1
      },
      "enableRightLabel": {
        "title": "Right Label",
        "description": "Enable the right label, which provides additional information.",
        "type": "boolean",
        "default": true,
        "order": 2
      }
    }
  },
  "linter": {
    "title": "Linter",
    "type": "object",
    "order": 4,
    "properties": {
      "enableWarnings": {
        "title": "Warnings",
        "description": "Enable linter warning messages within the editor.",
        "type": "boolean",
        "default": true,
        "order": 1
      },
      "enableInfo": {
        "title": "Info",
        "description": "Enable linter information messages within the editor.",
        "type": "boolean",
        "default": true,
        "order": 2
      }
    }
  }
}
